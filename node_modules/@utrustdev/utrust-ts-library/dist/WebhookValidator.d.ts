import { Event } from './types';
declare const WebhookValidator: (webhookSecret: string) => {
    validateSignature: ({ event_type, resource: { amount, currency, reference }, state, signature: eventSignature, }: Event) => boolean;
};
export default WebhookValidator;
